buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.1.0'
    }

    project.ext {
        buildToolsVersion = "23.0.1"
        minSdkVersion = 8
        targetSdkVersion = 23

        versionCode = 15
        versionName = "1.6.5"
    }
}

allprojects {
    repositories {
        jcenter()
    }
}
